// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("queue", "for queue support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("queue", "enable queue support", "no");

if (PHP_QUEUE != "no") {
	EXTENSION("queue", "queue.c");
}

